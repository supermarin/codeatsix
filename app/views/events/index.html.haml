- if @events.blank?
  %p No events in the system.
- else
  %p Events in the system:
  %ul
    - @events.each do |e|
      %li
        = link_to e.title, event_path(e)
        | 
        = link_to '[e]', edit_event_path(e) 
        |
        = link_to '[x]', event_path(e), :method => :delete, :confirm => "Ya sure?"

%p= link_to "Add new Event", new_event_path
